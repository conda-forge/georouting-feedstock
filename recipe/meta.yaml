{% set name = "georouting" %}
{% set version = "0.0.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/georouting-{{ version }}.tar.gz
  sha256: 583bc45ad47125e7c69afa19599091935b745d30259036cbcac7f4ad93fd47ad

build:
  entry_points:
    - georouting=georouting.cli:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pytest-runner
    - pip
  run:
    - python >={{ python_min }}
    - python-igraph
    - osmnx
    - polyline
    - googlemaps
    - geopandas
    - folium
    - mapclassify
    - matplotlib-base

test:
  imports:
    - georouting
  commands:
    - pip check
    - georouting --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/wybert/georouting
  summary: Geo routing for Python users
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - wybert
